#include <iostream>
#include <vector>
#include <string>

using namespace std;

int distancia(char a, char b);
char menor_distancia(vector<pair<int, char>>& dist, int j);
int Greedy_probabilista(string nombre_archivo, int deter_int);
int generate_random_value();
